---
import SiteLayout from "@/layouts/SiteLayout.astro";
import GamePlayer from "@/components/GamePlayer.astro";
import { getGame } from "@/data/games";

const game = getGame("guess-pokemon");
---

{game ? (
  <SiteLayout title={`${game.name} - Free Online Pokémon Guessing Game`} description={game.description} path={`/games/${game.slug}/`}>
    <h1 class="sr-only">{game.name}</h1>
    <GamePlayer game={game} />
  </SiteLayout>
) : (
  <div style="padding: 4rem; text-align: center;">
    <h2>Guess the Pokémon</h2>
    <p>Loading game configuration...</p>
  </div>
)}
